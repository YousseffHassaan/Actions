on: 
    repository_dispatch:
        types: [incident_report]
jobs:
    handle_incident:
        runs-on: ubuntu-latest
        steps:
            - name: Print
              uses: echo "Incident reported"

